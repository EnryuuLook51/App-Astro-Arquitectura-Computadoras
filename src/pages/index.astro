---
import UnitCard from "../components/UnitCard";
import Layout from "../layouts/Layout.astro";

const units = [
  {
    id: 1,
    title: "Unidad 1: Fundamentos",
    description:
      "Introducción a la arquitectura de computadoras y conceptos básicos del diseño digital",
    topics: 5,
    slug: "unidad-1",
  },
  {
    id: 2,
    title: "Unidad 2",
    description: "",
    topics: 5,
    slug: "unidad-2",
  },
  {
    id: 3,
    title: "Unidad 3",
    description: "",
    topics: 5,
    slug: "unidad-3",
  },
];

const team = [
  {
    name: "Saldarriaga Guerrero Franco",
    code: "0202314053",
  },
  {
    name: "Justyn lector Bustamante",
    code: "0202414021",
  },
  {
    name: "Yomona Santillan, Wilhelm Uriel",
    code: "0202414060",
  },
];

const technologies = [
  { name: "Astro", icon: "🚀" },
  { name: "React", icon: "⚛️" },
  { name: "TypeScript", icon: "📘" },
  { name: "Tailwind CSS", icon: "🎨" },
];
---

<Layout title="Arquitectura de Computadoras - UNS">
  <div
    class="min-h-screen bg-gradient-to-br from-white via-red-50 to-white relative overflow-hidden pt-20"
  >
    <!-- Animated background elements -->
    <div
      class="absolute inset-0 bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-red-100/20 via-transparent to-transparent"
    >
    </div>
    <div
      class="absolute top-20 left-1/4 w-96 h-96 bg-red-500/5 rounded-full blur-3xl animate-pulse"
    >
    </div>
    <div
      class="absolute bottom-0 right-1/4 w-96 h-96 bg-red-200/10 rounded-full blur-3xl animate-pulse delay-1000"
    >
    </div>

    <!-- Main Content -->
    <div class="container mx-auto px-4 py-8 md:py-16 max-w-7xl relative">
      <!-- Hero Section -->
      <div class="text-center mb-16 space-y-6">
        <div class="space-y-4">
          <h1
            class="text-4xl sm:text-5xl lg:text-6xl font-bold bg-gradient-to-r from-gray-900 via-red-600 to-gray-900 bg-clip-text text-transparent leading-tight"
          >
            Arquitectura de
            <span
              class="block bg-gradient-to-r from-red-400 to-red-600 bg-clip-text text-transparent"
            >
              Computadoras
            </span>
          </h1>
          <div class="space-y-2">
            <p class="text-lg md:text-xl text-gray-700 font-medium">
              Universidad Nacional del Santa
            </p>
            <p
              class="text-base md:text-lg text-gray-600 max-w-2xl mx-auto leading-relaxed px-4"
            >
              Repositorio de Videos Educativos - Ciclo IV • 2025
            </p>
          </div>
        </div>

        <!-- Decorative elements -->
        <div class="flex items-center justify-center gap-4 pt-4">
          <div
            class="w-12 md:w-16 h-px bg-gradient-to-r from-transparent to-red-300/70"
          >
          </div>
          <div class="w-2 h-2 bg-red-400 rounded-full animate-pulse"></div>
          <div
            class="w-12 md:w-16 h-px bg-gradient-to-l from-transparent to-red-300/70"
          >
          </div>
        </div>
      </div>

      <!-- Units Section -->
      <div class="max-w-5xl mx-auto mb-16">
        <div class="text-center mb-12">
          <h2 class="text-2xl md:text-3xl font-bold text-gray-900 mb-4">
            Unidades del Curso
          </h2>
          <p class="text-gray-700 text-base md:text-lg max-w-2xl mx-auto px-4">
            Explora cada unidad para acceder a los videos educativos organizados
            por temas
          </p>
        </div>

        <div class="grid gap-4 md:gap-6 lg:gap-8 max-w-4xl mx-auto">
          {
            units.map((unit) => (
              <UnitCard
                id={unit.id}
                title={unit.title}
                description={unit.description}
                topics={unit.topics}
                slug={unit.slug}
                client:load
              />
            ))
          }
        </div>
      </div>

      <!-- Team Section -->
      <div class="max-w-4xl mx-auto">
        <div
          class="bg-gray-50/80 backdrop-blur-sm rounded-2xl border border-red-200/50 p-6 md:p-8"
        >
          <h2
            class="text-2xl md:text-3xl font-bold text-gray-900 mb-6 text-center"
          >
            Equipo de Desarrollo
          </h2>
          <div class="grid md:grid-cols-2 gap-4">
            {
              team.map((member) => (
                <div class="bg-white/70 rounded-xl p-5 border border-red-200/50 hover:border-red-300/70 transition-all shadow-sm">
                  <div class="flex items-center gap-3">
                    <div class="w-12 h-12 bg-gradient-to-br from-red-100 to-red-200 rounded-full flex items-center justify-center border border-red-300/50 flex-shrink-0">
                      <svg
                        class="text-red-600"
                        width="24"
                        height="24"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                      >
                        <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" />
                        <circle cx="12" cy="7" r="4" />
                      </svg>
                    </div>
                    <div>
                      <p class="text-gray-900 font-semibold text-base">
                        {member.name}
                      </p>
                      <p class="text-gray-600 text-sm">Código: {member.code}</p>
                    </div>
                  </div>
                </div>
              ))
            }
          </div>
          <p class="text-center text-gray-500 text-sm mt-6">
            Ingeniería de Sistemas e Informática
          </p>
        </div>
      </div>

      <br />
      <br />
      <br />

      <!-- About Section -->
      <div class="max-w-4xl mx-auto mb-16">
        <div
          class="bg-gray-50/80 backdrop-blur-sm rounded-2xl border border-red-200/50 p-6 md:p-8"
        >
          <h2
            class="text-2xl md:text-3xl font-bold text-gray-900 mb-4 text-center"
          >
            Sobre este Proyecto
          </h2>
          <p
            class="text-gray-700 text-base md:text-lg leading-relaxed text-center mb-6"
          >
            Plataforma web desarrollada para centralizar y facilitar el acceso a
            los videos educativos del curso de Arquitectura de Computadoras,
            promoviendo el aprendizaje colaborativo.
          </p>

          <!-- Tecnologías utilizadas -->
          <div class="flex flex-wrap justify-center gap-3">
            {
              technologies.map((tech) => (
                <div class="bg-red-50 border border-red-200 rounded-lg px-4 py-2 flex items-center gap-2">
                  <span class="text-2xl">{tech.icon}</span>
                  <span class="text-gray-700 font-medium text-sm">
                    {tech.name}
                  </span>
                </div>
              ))
            }
          </div>
        </div>
      </div>
    </div>
  </div>
</Layout>
